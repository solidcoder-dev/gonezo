export function Home() {
  return (
    <section className="card">
      <h1>Local-first, multiplatform</h1>
      <p>
        React + Vite for web. Capacitor for mobile. Kotlin domain exposed as a
        plugin.
      </p>
      <p>
        This build uses HashRouter so it works in iOS/Android and static hosting.
      </p>
    </section>
  );
}
