# Platform Modules

Platform-specific adapters and plugin bridges.

Current implementation priority is Android:

- `android/plugin-core`
- `android/infrastructure`

iOS is a future target and should stay untouched unless a task explicitly asks for it:

- `ios/plugin-core`
