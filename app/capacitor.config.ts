import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gonezo.multiplatform',
  appName: 'gonezo-multiplatform',
  webDir: 'dist'
};

export default config;
